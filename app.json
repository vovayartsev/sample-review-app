{
    "name":"Advanced App",
    "addons": ["dockhero:standard"],
    "scripts": {
        "postdeploy": "yarn postdeploy"
    },
    "env": {
        "HEROKU_APP_NAME": {
            "required": true
        }
    },
    "buildpacks": [
        {
            "url": "https://github.com/heroku/heroku-buildpack-apt"
        },
        {
            "url": "heroku/nodejs"
        }
    ]
}
